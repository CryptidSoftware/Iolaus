namespace Iolaus.Tests.Routes
{
    public class RouterTests
    {
        // TODO: Need to add tests for Router. However, it seems like
        // Router shouldn't take an IConfigurationProvider, especially
        // if we want to pull configurations into their own library
    }
}